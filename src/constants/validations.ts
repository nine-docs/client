/* 이메일 유효성검사 기준
 * 1. @ : 반드시 하나의 @이 있어야 함
 * 2. [^\s@] : 공백이나 @가 아닌 문자들로 이루어진 문자열
 * 3. \. : 마침표
 */
export const EMAIL_PATTERN = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

/* 비밀번호 유효성검사 : 영문, 숫자 포함 6자리 이상 */
export const PASSWORD_PATTERN = /^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]{6,}$/;
